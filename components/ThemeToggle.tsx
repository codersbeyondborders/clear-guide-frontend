'use client'

// ThemeToggle is kept for internal app pages.
// The landing page does not use this component — it runs in light-only mode.
// This component is a no-op stub that maintains API compatibility.

export function ThemeToggle({ className }: { className?: string }) {
  // Light-only mode: no-op
  return null
}

export function ThemeSegmentedControl({ className }: { className?: string }) {
  // Light-only mode: no-op
  return null
}
